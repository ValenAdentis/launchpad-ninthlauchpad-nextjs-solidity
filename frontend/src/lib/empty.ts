// Intentionally empty stub used to satisfy optional dependencies that are
// imported (statically or dynamically) by wallet connector SDKs but never
// executed in this app.
export const toClientEvmSigner = undefined;